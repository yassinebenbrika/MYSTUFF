#include <stdio.h>

int main() {
    int f, n, i, check = 0;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    while (n <= 0) {
        printf("Incorrect value\n");
        printf("Enter the value of n: ");
        scanf("%d", &n);
    }
    for(i=2;i<=n/2;i++){
        
    }
    }

    return 0;
